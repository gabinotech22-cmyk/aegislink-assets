# AegisLink — public assets

Public hosting for social-media images (Instagram carousels, etc.).
Served via `raw.githubusercontent.com` so the Instagram Graph API can fetch
them at publish time — keeps these off the relay server (which costs per asset).

These are marketing images intended for public channels. No secrets here.
